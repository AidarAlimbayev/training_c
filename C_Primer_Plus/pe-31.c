/*pe-3.1*/
#include <stdio.h>
#include <math.h> 
int main (void)
{
    float x = 20;
    float per;
    float val;
    val = pow(2,64);
    per = val + x;

    // float one = 1;
    // float two = 2;
    // float rez;
    rez = two - one;
    printf("%.2f", rez);

    return 0;
}